<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xmlns:jpa="http://www.springframework.org/schema/data/jpa"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/context
       http://www.springframework.org/schema/context/spring-context.xsd
       http://www.springframework.org/schema/tx
       http://www.springframework.org/schema/tx/spring-tx.xsd
       http://www.springframework.org/schema/data/jpa
       http://www.springframework.org/schema/data/jpa/spring-jpa.xsd">

    <!-- Scan package level for Repository defined classes -->
    <jpa:repositories base-package="se.jsquad"/>

    <!-- Enable dependency injection for all component defined classes at the se.jsquad package level -->
    <context:component-scan base-package="se.jsquad"/>

    <context:property-placeholder location="classpath:/META-INF/property/app.properties"/>

    <!-- Setup JPA and transaction vendor implementation -->
    <bean class="org.apache.commons.dbcp2.BasicDataSource"
          destroy-method="close" id="dataSource">
    </bean>

    <tx:annotation-driven transaction-manager="transactionManager"/>

    <bean id="transactionManager" class="org.springframework.orm.jpa.JpaTransactionManager">
        <constructor-arg ref="entityManagerFactory"/>
    </bean>

    <bean id="entityManagerFactory"
          class="org.springframework.orm.jpa.LocalContainerEntityManagerFactoryBean">
        <property name="jpaVendorAdapter" ref="hibernateVendorAdapter"/>
    </bean>

    <bean id="hibernateVendorAdapter"
          class="org.springframework.orm.jpa.vendor.HibernateJpaVendorAdapter"/>

    <bean id="transactionTemplate"
          class="org.springframework.transaction.support.TransactionTemplate">
        <constructor-arg ref="transactionManager"/>
    </bean>

    <!-- Define the Startup database initiation -->
    <bean id="startupOpenBank" class="se.jsquad.business.StartupOpenBankComponentImpl"
          init-method="initiateDatabase" destroy-method="closeDatabase"/>

    <bean id="appProperty" class="se.jsquad.property.AppPropertyImpl" scope="singleton">
        <property name="version" value="${app.version}"/>
    </bean>

    <!-- Define mock values when intiating a new Client with DI -->
    <bean id="client" class="se.jsquad.entity.Client" scope="prototype">
        <property name="person" ref="person"/>
    </bean>

    <bean id="person" class="se.jsquad.entity.Person" scope="prototype">
        <property name="firstName" value="Mr. Spock"/>
        <property name="personIdentification" value="191212121212"/>
    </bean>
</beans>
